題目理解 : 將當前給定sr、sc周圍的非0元素都改變成當前image[sr][sc]的color

最直接想到DFS作法 : 由row-1、col-1、row+1、col+1四個方位去查找是否有相同的color，

因此跳出條件為 :  1.row、col超出邊界
                 2.當前顏色(也就是image[r][c])與將要被染成的顏色相同。
                 3.當前沒有顏色
                 


```
class Solution {
public:
    void dfs(vector<vector<int>>& image,int r,int c,int color){
        if(image[r][c] == color or image[r][c] == 0)return ;
        image[r][c] = color;

        if(r > 0)dfs(image,r-1,c,color);
        else if(c > 0)dfs(image,r,c-1,color);
        else if(r<image.size()-1)dfs(image,r+1,c,color);
        else if(c<image[0].size()-1)dfs(image,r,c+1,color);
    }
    vector<vector<int>> floodFill(vector<vector<int>>& image, int sr, int sc, int color) {
        for(int i = 0 ; i < image.size() ; i++){
            for(int j = 0 ; j < image.size() ; j++){
                
            }
        }
        dfs(image,sr,sc,color);
        return image;
    }
};
```
